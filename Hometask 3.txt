import Foundation

let myString = "abcdedeabf"                             // (1)                           

var letter = "a"                                        // (2)

if myString.contains(letter) {
  print("\(letter) is there")
} else {
  print("\(letter) isn`t there")
}

letter = "y"                                            // (3)

if myString.contains(letter) {
  print("\(letter) is there")
} else {
  print("\(letter) isn`t there")
}

var count = 0                                           // (4)

let symbolForSearch: Character = "a"

for char in myString {
    if char == symbolForSearch {
  count += 1 }
  }
  print("There is \(count) of symbol '\(symbolForSearch)' in string '\(myString)'")

let reversedString = String(myString.reversed())        // (5.1)

print(reversedString)

var reversedString2 = ""                                // (5.2) долго не мог вдуплить
for char in myString {                                  // как эта хуйня работает
    var symbol = "\(char)"  
    reversedString2 = symbol + reversedString2
}

print(reversedString2)

let firstPart = "abc"                                    // (6)

let secondPart = "def"

let union1 = firstPart + secondPart

let union2 = "\(firstPart)\(secondPart)"

print(union1); print(union2)

var automobile = "car"                                  // (7.1)

var str1 = ""

let letterForReplace: Character = "r"

for char in automobile {
  if char == letterForReplace {
    str1 += "p"
  } else {
    str1 += "\(char)"
  }
}
automobile = str1; print(automobile)

var automobile2 = "car"                                 // (7.2)

var str = automobile2.replacingOccurrences(of: "r", with: "p")

print(str)

let helloJura = "Привет Юра"                            // (P.S.)

var splitJura = helloJura.components(separatedBy: " ")

var firstWord:String = splitJura[0]

var lastWord:String = splitJura[1]

print(firstWord); print(lastWord)

